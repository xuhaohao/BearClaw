<UserControl x:Class="BearClaw.BlackMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid Margin="4" IsReadOnly="False" x:Name="dgMain" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding StrName,Mode=TwoWay}" Width="*"/>
                <DataGridTextColumn Header="获取时间" Binding="{Binding GetTime}" IsReadOnly="True" Width="*"/>
                <!--<DataGridTextColumn Header="单价" Binding="{Binding Price}" MinWidth="100"/>-->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
